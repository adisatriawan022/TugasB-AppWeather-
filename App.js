import React from 'react';
import {AppRegistry, StyleSheet, View, Text, TextInput, Button, Image} from 'react-native';

import Layout from './src/Comp/Weather2';

export default class App extends React.Component {
  render() {
    return (
      <Layout />
    );
  }
}
